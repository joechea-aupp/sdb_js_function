const sum = (a, b) => a + b;

const subtract = function (a, b) {
  return a - b;
};

const createUser = (firstName, lastName, email) => ({
  firstName: firstName,
  lastName: lastName,
  email: email,
});
